"""Shared library for the LLM-simulator benchmarking thesis.

The same environment wrapper, policy set, tabular Q-learning routines and
rank-correlation metrics are used by every experiment under ``experiments/``.
"""
